# إصلاح الصفحة البيضاء / Fix White Page

## المشكلة / Problem:
الصفحة بيضاء رغم أن workflow نجح ✅

## الحلول / Solutions:

### 1. تحقق من Console المتصفح

1. افتح الموقع: `https://zalwagdani.github.io/media-page/`
2. اضغط **F12** (أو Cmd+Option+I على Mac)
3. اذهب إلى تبويب **Console**
4. ابحث عن أخطاء (أحمر)

**أرسل لي أي أخطاء تراها**

---

### 2. تحقق من Network

1. في Developer Tools (F12)
2. اذهب إلى تبويب **Network**
3. أعد تحميل الصفحة (F5)
4. ابحث عن ملفات فاشلة (أحمر)

**الملفات المهمة:**
- `index.html` - يجب أن يكون ✅
- `main.jsx` أو `main.js` - يجب أن يكون ✅
- ملفات CSS - يجب أن تكون ✅

---

### 3. تحقق من المسارات

المشكلة الشائعة: المسارات المطلقة لا تعمل مع GitHub Pages.

**تم إصلاح `index.html`** - ارفع التغييرات:

```bash
cd "/Users/zalwagdani/Media Page"
git add index.html
git commit -m "Fix script path in index.html"
git push
```

---

### 4. تحقق من vite.config.js

تأكد من أن `base` صحيح:

```javascript
base: process.env.NODE_ENV === 'production' ? '/media-page/' : '/',
```

---

### 5. تحقق من بناء الملفات

جرب بناء محلي:

```bash
cd "/Users/zalwagdani/Media Page"
npm run build
npm run preview
```

إذا عمل محلياً، المشكلة في النشر.
إذا لم يعمل، المشكلة في الكود.

---

### 6. حل بديل: استخدام 404.html

أنشئ ملف `404.html` في مجلد `public`:

```html
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Redirecting...</title>
    <script>
      sessionStorage.redirect = location.href.split('/').slice(-1)[0];
      location.replace('/media-page/');
    </script>
  </head>
  <body></body>
</html>
```

---

### 7. تحقق من ملفات dist

بعد البناء، تحقق من مجلد `dist`:

```bash
cd "/Users/zalwagdani/Media Page"
npm run build
ls -la dist/
```

يجب أن ترى:
- `index.html`
- `assets/` (مجلد)
- ملفات أخرى

---

## 🔍 خطوات التشخيص / Diagnostic Steps:

### الخطوة 1: افتح Console
- اضغط F12
- اذهب إلى Console
- ابحث عن أخطاء

### الخطوة 2: تحقق من Network
- تبويب Network
- أعد تحميل الصفحة
- ابحث عن ملفات فاشلة

### الخطوة 3: تحقق من Source
- تبويب Elements أو Sources
- ابحث عن `<div id="root">`
- تحقق من محتواه

---

## 📋 قائمة التحقق / Checklist:

- [ ] فتحت Console (F12) وقرأت الأخطاء
- [ ] فتحت Network وفحصت الملفات
- [ ] رفعت التغييرات على `index.html`
- [ ] انتظرت 2-3 دقائق بعد الرفع
- [ ] امسحت cache المتصفح
- [ ] جربت في نافذة خاصة

---

## 🆘 أرسل لي:

1. **لقطة شاشة من Console:**
   - F12 → Console
   - أي أخطاء (أحمر)

2. **لقطة شاشة من Network:**
   - F12 → Network
   - الملفات الفاشلة

3. **محتوى `<div id="root">`:**
   - F12 → Elements
   - ابحث عن `#root`
   - ما بداخله؟

---

## ⚡ حل سريع:

جرب هذا:

1. **امسح cache تماماً:**
   - Chrome: Settings → Privacy → Clear browsing data
   - أو استخدم نافذة خاصة

2. **جرب الرابط مباشرة:**
   ```
   https://zalwagdani.github.io/media-page/index.html
   ```

3. **تحقق من GitHub Actions:**
   - هل workflow نجح حقاً؟
   - هل تم رفع الملفات إلى `gh-pages` branch؟

---

## 🔧 إذا لم يعمل بعد:

أرسل لي:
1. لقطة شاشة من Console (F12)
2. لقطة شاشة من Network (F12)
3. أي رسائل خطأ تراها

سأساعدك في حل المشكلة!
